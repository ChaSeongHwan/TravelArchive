# ==============================================================================
# DB 런타임 데이터 (컨테이너가 생성하는 데이터 — 내용물 제외, 폴더 존재만 추적)
# ==============================================================================
# PostgreSQL 데이터
db/data/**
!db/data/.gitkeep

# Redis 데이터
db/redis_data/**
!db/redis_data/.gitkeep

# ==============================================================================
# 환경 변수 (절대 커밋 금지)
# ==============================================================================
setting/.env
.env
